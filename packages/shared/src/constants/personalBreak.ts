/**
 * Личная отлучка — сотрудник ненадолго отходит по своим делам, не закрывая
 * смену. Заявляет срок сам, потолок общий для всех: тридцать минут хватает
 * на большинство личных дел, а более долгое отсутствие — это уже не отлучка,
 * и для него есть закрытие смены.
 */
export const MAX_PERSONAL_BREAK_MINUTES = 30;

/** Варианты срока в форме — шагом в 5 минут до потолка. */
export const PERSONAL_BREAK_DURATION_OPTIONS = [10, 15, 20, 25, 30] as const;
