'use client';

import { redirect } from 'next/navigation';

/**
 * Финансы.
 *
 * Отдельного финансового модуля нет: выручка, себестоимость, маржа и фонд
 * зарплаты уже сведены в разделе «Отчёты». Дублировать те же цифры под другим
 * названием — верный способ получить два расходящихся источника правды,
 * поэтому раздел перенаправляет в «Отчёты».
 */
export default function FinancePage(): never {
  redirect('/reports');
}
