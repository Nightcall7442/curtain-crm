'use client';

import type { ReactElement } from 'react';

import { StubPage } from '@/components/ui/StubPage';

/**
 * Раздел «Склад тканей» не реализован.
 *
 * Оставлен в навигации осознанно: он был в структуре, согласованной с
 * заказчиком, но модели данных под него в системе нет. Показывать здесь
 * правдоподобные цифры было бы хуже, чем не показывать ничего — такой экран
 * не отличить от рабочего, и по нему начинают принимать решения.
 */
export default function Page(): ReactElement {
  return (
    <StubPage
      title="Склад тканей"
      reason="Складского учёта в системе нет. Закупки фиксируются по каждому заказу и дают себестоимость, но остатков рулонов, приходов и списаний система не ведёт."
      requires={['Таблица остатков по номенклатуре', 'Приход и списание материалов', 'Связь списания с раскроем конкретного заказа']}
    />
  );
}
