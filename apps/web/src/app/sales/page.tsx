'use client';

import type { ReactElement } from 'react';

import { StubPage } from '@/components/ui/StubPage';

/**
 * Раздел «Продажи» не реализован.
 *
 * Оставлен в навигации осознанно: он был в структуре, согласованной с
 * заказчиком, но модели данных под него в системе нет. Показывать здесь
 * правдоподобные цифры было бы хуже, чем не показывать ничего — такой экран
 * не отличить от рабочего, и по нему начинают принимать решения.
 */
export default function Page(): ReactElement {
  return (
    <StubPage
      title="Продажи"
      reason="Воронка продаж требует учёта обращений и коммерческих предложений — в системе фиксируются только оформленные заказы. Готовые цифры по выручке есть в разделах «Отчёты» и «Финансы»."
      requires={['Учёт лидов и обращений', 'Стадии сделки до оформления заказа', 'Источник обращения для расчёта конверсии']}
    />
  );
}
