'use client';

import type { ReactElement } from 'react';

import { StubPage } from '@/components/ui/StubPage';

/**
 * Раздел «Клиенты» не реализован.
 *
 * Оставлен в навигации осознанно: он был в структуре, согласованной с
 * заказчиком, но модели данных под него в системе нет. Показывать здесь
 * правдоподобные цифры было бы хуже, чем не показывать ничего — такой экран
 * не отличить от рабочего, и по нему начинают принимать решения.
 */
export default function Page(): ReactElement {
  return (
    <StubPage
      title="Клиенты"
      reason="Отдельной сущности «клиент» в системе нет: имя и телефон хранятся прямо в заказе. Историю обращений и повторные продажи собрать пока не из чего."
      requires={['Таблица клиентов со связью «клиент — заказы»', 'Объединение заказов по нормализованному номеру телефона', 'Учёт обращений, чтобы считать конверсию']}
    />
  );
}
